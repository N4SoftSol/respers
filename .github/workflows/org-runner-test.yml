name: Org Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - shared

    steps:
      - name: Verify Runner
        run: |
          echo "Runner Test"
          hostname
          whoami
          pwd
